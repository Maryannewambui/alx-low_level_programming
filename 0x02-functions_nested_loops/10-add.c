#include "main.h"
/**
 *add - add two integers and returns results
 *@d: int added to e
 *@e: int added to d
 *
 *Return: sum of d and e
 */
int add(int d, int e)
{
	int f;

	f = d + e;
	return (f);
}
